import("fardlex")  as lex
import("std/fs")   as fs
import("std/list") as list

let src = fs.read_text("apps/fardlex.fard")
let tokens = lex.tokenize(src)
{ n: list.len(tokens), first: list.get(tokens, 0), second: list.get(tokens, 1), third: list.get(tokens, 2) }
