import("../packages/audio-core/src/synth") as synth
import("../packages/audio-core/src/export") as audio_export

let sample_rate = 44100
let samples = synth.sine(440.0, 2.0, sample_rate)
audio_export.write_wav("out/tone_440.wav", "tone_440", sample_rate, 1, samples)
