import("../packages/fard_hm/hm") as hm

let env = {}

let add_like = {
  t: "node_fn",
  params: ["x", "y"],
  body: { t: "var", name: "x" }
}

let r = hm.infer(add_like, env, {}, 0)

test "hm must infer multi-arg parser helpers next" { r.ty.t == "tfun" }
