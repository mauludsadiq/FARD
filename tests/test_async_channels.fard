import("../packages/async-channels/main") as ch

let c = ch.channel()

test "send and recv"  { let _ = ch.send(c, 42) in ch.recv(c) == 42 }
test "is_closed"      { ch.is_closed(c) == false }
test "close"          { let _ = ch.close(c) in ch.is_closed(c) == true }
