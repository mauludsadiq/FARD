import("std/ast") as ast
import("../packages/fard_lower/lower") as lower
import("std/list") as list

let nodes = ast.parse("import(\"std/list\") as list\nlet x = list.len([])")
let fir = list.map(nodes, fn(n) { lower.lower(n) })
fir
