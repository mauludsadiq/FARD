import("../packages/async-core/main") as a

test "resolved"      { a.resolved(42) == 42 }
test "spawn await"   { a.await(a.spawn(fn() { 1 + 1 })) == 2 }
test "all"           { a.all([a.spawn(fn() { 1 }), a.spawn(fn() { 2 })]) == [1, 2] }
test "sleep returns error record" { a.sleep(0).t == "err" }
