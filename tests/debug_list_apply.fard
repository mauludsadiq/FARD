import("std/list") as list
import("std/str") as str
import("std/type") as type

test "type of str.replace" { type.of(str.replace) }
let result = list.apply(str.replace, ["hello world", "world", "FARD"])
test "list.apply builtin" { result == "hello FARD" }
