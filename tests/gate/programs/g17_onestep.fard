import("../../../packages/onestep/main") as os
import("std/list") as list

let root = os.node("root", 0)
let a = os.add_child(os.node("a", 1), os.node("c", 3))
let b = os.add_child(os.node("b", 2), os.node("d", 4))
let r = os.add_child(os.add_child(root, a), b)

r |> os.collapse |> os.node_count
