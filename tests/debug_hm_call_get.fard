import("../packages/fard_hm/hm") as hm

let call_node = {
  t: "call",
  fn: { t: "get_field", obj: { t: "var", name: "x" }, field: "foo" },
  args: []
}
let env = {}
let r = hm.infer(call_node, env, {}, 0)
test "ok" { r.ty == r.ty }
