import("../packages/fard_hm/hm") as hm
import("../packages/fard_parse/parse") as parse
import("std/list") as list

let defs = parse.parse_program("fn add(x, y) { x + y }")
let r = hm.infer_program(defs)
test "ok" { r.ok == true }
test "add in env" { rec.get(r.env, "add") == rec.get(r.env, "add") }
