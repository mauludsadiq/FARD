import("std/list") as list
import("std/record") as rec
import("std/type") as type

let set_fn = rec.get(rec, "set")
test "type of set_fn" { type.of(set_fn) == "function" }
let result = list.apply(set_fn, [{}, "x", 42])
test "result" { result.x == 42 }
