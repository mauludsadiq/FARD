import("../packages/fard_parse/parse") as parse
import("std/list") as list

let src = "fn f(x) { match x { \"a\" => 1 , _ => { t : \"err\" } } } fn g(x) { x }"
let toks = parse.tokenize(src)
let defs = parse.parse_program(src)
test "two defs" { list.len(defs) == 2 }
